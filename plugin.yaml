name: weixin-prefix-router
version: 1.0.0
description: "Route WeChat (weixin) messages to different Hermes profiles by fixed prefix. Messages starting with a configured prefix (e.g. @coder) are routed to that profile's agent; all other messages go to the default Hermes agent."
author: "Harry"
hooks:
  - pre_gateway_dispatch
